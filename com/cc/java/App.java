package com.cc.java;

public class App {                                              // Klasse muss wie die Datei benannt sein
    public static void main( String[] args ) {                  // Methode (Funktion)  die main Methode ist Pflicht, Entry Point (Einstiegspunkt)
        System.out.println( "Hello, Test World!" );             // Call (Aufruf) einer definierten Methode (println)
    }
}
